@model MarketWebApp.PlaceOrderViewModel
@{
    ViewData["Title"] = "PlaceOrder";
}
@if (ViewBag.Message != null)
{
    <p>@ViewBag.Message</p>
}

else
{
    @* <h3>Shopping Cart:</h3>
    <ul>
        @foreach (var productCart in Model.ShoppingCart.ProductCarts)
        {
            <li>@productCart.Product.Name - Quantity: @productCart.Quantity</li>
        }
    </ul>

    <h2>Place Order</h2>
*@



    <!-- Checkout Section Begin -->
    <section class="checkout spad">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                </div>
            </div>
            <div class="checkout__form">
                <h4>Billing Details</h4>
                <form asp-controller="Order" asp-action="ConfirmOrder">
                    <div class="row">
                        <div class="col-lg-8 col-md-6">
                            <div class="row">
                            </div>

                            <div class="checkout__input">
                                <p>Address<span>*</span></p>
                                @using (Html.BeginForm("ConfirmOrder", "Order", FormMethod.Post))
                                {
                                    @Html.LabelFor(m => m.SelectedLocationId, "Select Location:")
                                    <br />
                                    @Html.DropDownListFor(m => m.SelectedLocationId, new SelectList(Model.Locations, "ID", "Name"))
                                }
                                <br />
                                <br />
                                <input type="text" placeholder="Street Address" class="checkout__input__add">
                                <input type="text" placeholder="Apartment, suite, unite ect (optinal)">
                            </div>
                            <div class="row">
                            </div>

                        </div>
                        <div class="col-lg-4 col-md-6">
                            <div class="checkout__order">
                                <h4>Your Order</h4>
                                <div class="checkout__order__products">Products <span>Total</span></div>
                                <ul>
                               

                                    @foreach (var productCart in Model.ShoppingCart.ProductCarts)
                                    {
                                        <li>@productCart.Product.Name <span>@(((productCart.Product.Price - (productCart.Product.Price * productCart.Product.Discount / 100)) * productCart.Quantity))</span></li>
                                    }
                                </ul>
                                @{
                                    double sum = 0;
                                }
                                @foreach (var productCart in Model.ShoppingCart.ProductCarts)
                                {
                                     sum += (productCart.Product.Price - (productCart.Product.Price * productCart.Product.Discount / 100))*productCart.Quantity;
                                }
                                <div class="checkout__order__total">Total <span>@sum L.E</span></div>
                                <div class="checkout__input__checkbox">
                                    <label for="acc-or">
                                        Create an account?
                                        <input type="checkbox" id="acc-or">
                                        <span class="checkmark"></span>
                                    </label>
                                </div>
                                <p>
                                    Lorem ipsum dolor sit amet, consectetur adip elit, sed do eiusmod tempor incididunt
                                    ut labore et dolore magna aliqua.
                                </p>
                                <div class="checkout__input__checkbox">
                                    <label for="payment">
                                        Check Payment
                                        <input type="checkbox" id="payment">
                                        <span class="checkmark"></span>
                                    </label>
                                </div>
                                <div class="checkout__input__checkbox">
                                    <label for="paypal">
                                        Paypal
                                        <input type="checkbox" id="paypal">
                                        <span class="checkmark"></span>
                                    </label>
                                </div>
                                <button type="submit" class="site-btn">PLACE ORDER</button>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </section>
    <!-- Checkout Section End -->
    @*     @using (Html.BeginForm("ConfirmOrder", "Order", FormMethod.Post))
    {
        @Html.LabelFor(m => m.SelectedLocationId, "Select Location:")
        @Html.DropDownListFor(m => m.SelectedLocationId, new SelectList(Model.Locations, "ID", "Name"))
        <br />
        <input type="submit" value="Place Order" />
    } *@


}